## Commands
> cd frontend
> npm run dev
> python backend/app.py
> curl -Uri http://localhost:8000/health -UseBasicParsing
> wscat -c ws://localhost:8000/stream
> docker build -t <image_name:tag> .
> docker run -p <host_port>:<container_port> <image_name>:<tag>
>